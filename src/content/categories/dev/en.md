---
catSlug: dev
locale: en
name: For developers
blurb: Encode, decode, tidy up names
title: Developer Tools Online — Base64, URL Encoding, Escaping
description: Base64 and URL encoding, escaping special characters, stripping HTML tags, camelCase and snake_case. Free, no sign-up, everything runs in your browser.
h1: Developer tools
---

The small jobs that come up every day: encode a string, decode somebody else's, strip the tags out of a chunk of markup, rewrite names for another language.

Everything runs right in your browser. Keys, tokens and other people's code are never sent anywhere and never stored — which matters most here, where the clipboard keeps filling up with things that must not be shown.
