/* ОДНА СТРОКА О ТОМ, ЧТО ИНСТРУМЕНТ ДЕЛАЕТ.
 *
 * Единственное место, где решается, какая подпись окажется на карточке. Его зовут и страница
 * рубрики, и плитки «попробуйте ещё», и проверка check:cards -- поэтому правило одно на всех,
 * а не три похожих в трёх местах.
 *
 * ПРАВИЛО РОВНО ОДНО: подпись написана в шапке страницы, в поле summary. Никаких исключений,
 * никакой сборки на лету.
 *
 * ПОЧЕМУ НЕ СОБИРАЕМ САМИ -- разобрано на живом примере, а не из осторожности. Замысел был
 * такой: у конвертеров величин название «Километры в мили» и так всё говорит, а множитель в
 * шапке уже лежит, значит подпись можно складывать: «1 км = 0,621 мили». Тогда при сотне
 * новых пар писать не пришлось бы ничего.
 *
 * Сложилось вот что:
 *
 *     1 см = 0,394 дюйм        (нужно «дюйма»)
 *     1 км = 0,621 миль        (нужно «мили»)
 *
 * После дробного числа русский требует родительного единственного, и нужной формы в шапках
 * нет: там лежат «дюйм», «миль», «фунт», «гал» -- кто в именительном, кто в родительном
 * множественном. Добавить ещё одно поле под эту форму не выход: в польском после дробного
 * своя форма, в арабском счёт зависит от самого числа, и каждый новый язык приносил бы новое
 * правило в общий сборщик -- то есть переводы снова растеклись бы за пределы шапок.
 *
 * ГРАММАТИКА НЕ СОБИРАЕТСЯ. Фразу на языке пишет тот, кто на нём говорит, и лежит она там же,
 * где остальной текст страницы. А забывчивость ловит npm run check:cards -- без подписи
 * проверка падает, и страница не уедет на сайт пустой.
 */

/**
 * Подпись для карточки.
 * @param данные  data записи коллекции (tools или mediaTools)
 * @returns строка; пустая -- значит подписи нет, и это ловит check:cards
 */
export function подпись(данные) {
  return данные.summary || '';
}
